This directory contains a src directory with a makefile.
The makefile builds 2 versions according the a reconstruction of the makefile templates used in the dry run system.

I've named the binaries solitaire_debug and solitaire_release.

Commandlines and their inputs (substitute _debug or _release as needed):

"Good inputs" (key 25 or fewer chars):

./solitaire ../testData/keya_good < input.txt
./solitaire ../testData/keyb_good < input.txt
./solitaire ../testData/keyc_good < input.txt
./solitaire ../testData/keye_good < input.txt

Bad inputs (key longer than 25):

./solitaire ../testData/keyf_bad < input.txt

(Causes a segmentation fault/DOS)
