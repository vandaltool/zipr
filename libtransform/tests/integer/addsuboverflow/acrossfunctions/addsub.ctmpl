#include <stdio.h>

#TYPE1# doAdd(#TYPE1# a, #TYPE2# b)
{
  volatile #TYPE1# result = a + b;
  printf ("result: #FORMAT_SPECIFIER#\n", result);
  return result;
}

// pass in 2 arguments
main(int argc, char **argv)
{
  #TYPE1# x, result;
  #TYPE2# y;

  if (argc != 3)
  {
    fprintf(stderr,"usage: %s <arg1> <arg2>\n", argv[0]);
    exit(1);
  }

  x = #FUNCTION_NAME1#(argv[1], NULL, 10);
  y = #FUNCTION_NAME2#(argv[2], NULL, 10);
  result = doAdd(x, y);
}

