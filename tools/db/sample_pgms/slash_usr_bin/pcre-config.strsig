      ;;
#!/bin/sh
  *BSD*)
  case "$1" in
  case $1 in
case `uname -s` in
    --cflags | --cflags-posix)
      echo 8.12
      echo $exec_prefix
      echo $includes 
      echo -L${prefix}/lib/i386-linux-gnu$libR -lpcre
        echo -L${prefix}/lib/i386-linux-gnu$libR -lpcrecpp -lpcre
      echo -L${prefix}/lib/i386-linux-gnu$libR -lpcreposix -lpcre
      echo $prefix
        echo "${usage}" 1>&2
      echo "${usage}" 1>&2
      else
    --exec-prefix=*)
    --exec-prefix)
        exec_prefix=$optarg
      exec_prefix=$optarg
exec_prefix=${prefix}
exec_prefix_set=no
      exec_prefix_set=yes
      exit 1
      fi
if test $# -eq 0; then
      if test $exec_prefix_set = no ; then
      if test ${prefix}/include != /usr/include ; then
      if test yes = yes ; then
if test yes = yes ; then
        includes=-I${prefix}/include
  libR=" -R${prefix}/lib/i386-linux-gnu"
  libR=" -Wl,-R${prefix}/lib/i386-linux-gnu"
    --libs)
    --libs-cpp)
    --libs-posix)
  *) optarg= ;;
  -*=*) optarg=`echo "$1" | sed 's/[-_a-zA-Z0-9]*=//'` ;;
    --prefix=*)
    --prefix)
      prefix=$optarg
prefix=/usr
  *SunOS*)
  usage="Usage: pcre-config [--prefix] [--exec-prefix] [--version] [--libs] [--libs-posix] [--cflags] [--cflags-posix]"
  usage="Usage: pcre-config [--prefix] [--exec-prefix] [--version] [--libs] [--libs-posix] [--libs-cpp] [--cflags] [--cflags-posix]"
    --version)
while test $# -gt 0; do
